[project]
name = "harness-devflow"
version = "1.0.0"
description = "Provider-neutral, evidence-driven development workflow for coding agents"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py310"
